# Sample Images

Coloque aqui imagens de exemplo que ficarão disponíveis no endpoint `GET /v1/samples`.

Formatos aceitos:
- `.jpg`, `.jpeg`, `.png`, `.bmp`, `.tif`, `.tiff`

A UI Vue lista os arquivos deste diretório para o usuário escolher sem upload local.
